package regex

const (
    kleeneStar = '*'
    orOperator = '+'
    lBracket   = '('
    rBracket   = ')'

    fewArgumentsErrorFormat = "few arguments for %v operator"
    invalidParenthesesError = "invalid parentheses"
)
